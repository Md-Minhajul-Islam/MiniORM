# Copy to .env in this folder (repo root) and adjust for your machine.
# Do not commit .env — it is listed in .gitignore.

MINIORM_CONN=Server=.;Database=MiniORMDB;Trusted_Connection=True;TrustServerCertificate=True
